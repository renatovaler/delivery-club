{
  "name": "ProcessedEvent",
  "type": "object",
  "properties": {
    "stripe_event_id": {
      "type": "string",
      "description": "ID do evento do Stripe (evt_...)"
    },
    "processed_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp de quando o evento foi processado"
    }
  },
  "required": [
    "stripe_event_id"
  ]
}